#include "mathlib.h"

	/*matrix * multiplyMatrices(matrix * matrix1, matrix * matrix2) {

		matrix * matrix1Ptr = matrix1;
		matrix * matrix2Ptr = matrix2;

		matrix outputMatrix();

	
		for (unsigned int i = 1; i < 5; i++) {
			for (unsigned int j = 1; j < 5; i++) {

			}
		}
	}*/
