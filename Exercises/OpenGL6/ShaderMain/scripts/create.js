// CURRENTLY IMPLEMENTED:
    //ClearLog();
    //Log("STRING TO LOG");
    //new Light();
    //new Teapot(INT SIZE, BOOLEAN WIREFRAME, INT POSITION_X, INT POSITION_Y, INT POSITION_Z);
    //Rotate("NODENAME", VECTOR_X, VECTOR_Y, VECTOR_Z, ANGLE);

function Create()
{   
    new Teapot(50, true, 0, -200, 0);
}

Create();